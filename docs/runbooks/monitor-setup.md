# Monitor setup (Part B)

1. Copy `.env.example` values into `.env` (create locally — not committed)
2. `npm install && npm run monitor`
3. Point `RPC_URL` at your testnet or fork

See Part B in `README.md`.
